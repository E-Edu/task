# Please fill out with content

version: '3.7'

services:
  task_microservice_mariadb:
    image: mariadb
    container_name: task_microservice_mariadb
    environment:
      MYSQL_USER: "e-edu"
      MYSQL_PASSWORD: "e-edu"
      MYSQL_DATABASE: task_microservice_db
    ports:
      - 3306:3306
    volumes:
      - task_microservice_mariadb_data:/var/lib/

volumes:
  task_microservice_mariadb_data:
    
